<?xml version="1.0" encoding="UTF-8"?>
<odoo>

        <record id="purchase.purchase_rfq" model="ir.actions.act_window">
            <field name="context">{'purchase_rfq': True}</field>
        </record>

        <record id="purchase_order_form" model="ir.ui.view">
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <field name="currency_id" position="after">
                    <field name="picking_state"
                           attrs="{'invisible':[('picking_state','=', 'draft')]}"/>
                </field>
            </field>
        </record>

        <record id="purchase_order_tree" model="ir.ui.view">
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_tree"/>
            <field name="arch" type="xml">
                <field name="state" position="after">
                    <field name="picking_state"
                           invisible="context.get('purchase_rfq')"/>
                </field>
            </field>
        </record>

        <record id="purchase_order_search" model="ir.ui.view">
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.view_purchase_order_filter"/>
            <field name="arch" type="xml">
                <search>
                    <filter name="incomplete_orders" string="Incomplete Orders" domain="['|',('picking_state','=','not_received'),('picking_state','=','partially_received')]"/>
                    <filter name="order_state" string="Order State" context="{'group_by':'picking_state'}" invisible="context.get('purchase_rfq')"/>
                </search>
            </field>
        </record>

</odoo>
